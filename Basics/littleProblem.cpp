#include "basicsHeader.h"
#include <iostream>

using namespace std;

void product() {
    int x, y; //Variable declaration
    cin >> x >> y; //Read input
    int result = x * y;
    cout << result << endl; //Make output
}

void addition() {
    int x, y, z;
    cin >> x >> y >> z;
    int result = x + y + z;
    cout << result << endl;
}